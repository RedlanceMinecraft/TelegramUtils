# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Mod Properties
    mod_version = 1.0.0
    maven_group = org.redlance
    archives_base_name = tgutils

# Dependencies
    tdlight_version = 3.4.0+td.1.8.26
